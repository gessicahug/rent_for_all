<h1 class="text-center m-5">ENCONTRE O QUE VOCÊ PRECISA:</h1>
<div class="container mt-1 mb-5 pt-3 pb-3">
<% @items.each do |item| %>
<%= link_to item_path(item), class: 'text-decoration-none' do  %>
<div class="card-product m-3">
  <%= image_tag 'tool.png' %>
  <div class="card-product-infos">
    <h2><%=item.name%></h2>
    <p><%=item.description%> <strong>Valor de aluguel por dia : R$<%=item.price%></strong></p>
  </div>

</div>
<% end %>
<% end %>
</div>
